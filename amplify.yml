version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site — checking changed files..."
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
    exclude:
      - 'sidekick/**'
      - 'node_modules/**'
  cache:
    paths: []
