{
  "name": "securebydezign-pdf-api",
  "version": "1.0.0",
  "description": "Stripe webhook + session API for PDF email and download (S3-hosted site)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "zip": "npm install --omit=dev && zip -r ../lambda-deploy.zip . -x '*.git*' -x '*.DS_Store' && ls -lh ../lambda-deploy.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-ses": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "stripe": "^17.3.0"
  }
}
