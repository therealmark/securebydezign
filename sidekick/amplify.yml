version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Sidekick — static site, no build step"
    build:
      commands:
        - echo "Done"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
